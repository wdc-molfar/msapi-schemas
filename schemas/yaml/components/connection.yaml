title: Підсхема "connection"
description: Налаштування з’єднання
subschema: msapi.components.connection         
type: object


required:
  - url

patternProperties:  
  '^url$':
    title: url
    description: Налаштування посилання
    type: string
    format: uri-reference

errorMessage:
  type: Connection options should be an object.
  required: Connection url is required.
  properties:
    url: Connection url should be a valid RabbitMQ connection url.
